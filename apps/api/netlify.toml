# https://ntl.fyi/file-based-build-config
[build]
command = "APP_NODE_ENV=production APP_COMMIT_REF=${COMMIT_REF} nx run @tally/api:build"
functions = "apps/api/dist/functions"
publish = "apps/api/dist/publish"
